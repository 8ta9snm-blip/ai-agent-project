# دليل نشر المشروع على Railway

## المشروع جاهز! 🎉

تم رفع الكود بنجاح على GitHub على الرابط التالي:
**https://github.com/8ta9snm-blip/ai-agent-project**

## الخطوات لنشر المشروع على Railway

### 1. تسجيل الدخول إلى Railway

1. افتح موقع Railway: **https://railway.app**
2. اضغط على **Login** أو **Sign up**
3. سجل دخول باستخدام حساب GitHub الخاص بك (8ta9snm@gmail.com)

### 2. إنشاء مشروع جديد

1. بعد تسجيل الدخول، اضغط على **New Project**
2. اختر **Deploy from GitHub repo**
3. إذا طُلب منك، اسمح لـ Railway بالوصول إلى مستودعات GitHub الخاصة بك
4. ابحث عن المستودع: **ai-agent-project**
5. اضغط على المستودع لاختياره

### 3. إعدادات النشر

Railway سيكتشف تلقائياً:
- ✅ ملف `Procfile` (يحدد كيفية تشغيل التطبيق)
- ✅ ملف `runtime.txt` (يحدد نسخة Python)
- ✅ ملف `requirements.txt` (يحدد المكتبات المطلوبة)

### 4. النشر التلقائي

1. Railway سيبدأ تلقائياً في بناء ونشر المشروع
2. يمكنك متابعة عملية النشر من خلال **Deployments** في لوحة التحكم
3. عند اكتمال النشر، سيظهر لك حالة **Success** ✅

### 5. إعدادات إضافية (اختيارية)

#### إضافة متغيرات البيئة (Environment Variables)
إذا كان المشروع يحتاج إلى مفاتيح API أو إعدادات خاصة:
1. اذهب إلى **Variables** في لوحة التحكم
2. أضف المتغيرات المطلوبة

#### تفعيل Domain مخصص
1. اذهب إلى **Settings** > **Domains**
2. يمكنك إضافة domain مخصص أو استخدام الـ domain المجاني من Railway

### 6. مراقبة التطبيق

- **Logs**: لعرض سجلات التطبيق والأخطاء
- **Metrics**: لمراقبة استخدام الموارد
- **Deployments**: لعرض تاريخ النشر

## ملاحظات مهمة

### الملفات المهمة في المشروع:

1. **Procfile**: يحدد أمر التشغيل
   ```
   worker: python3 ai_agent_logic.py
   ```

2. **runtime.txt**: يحدد نسخة Python
   ```
   python-3.11.0
   ```

3. **requirements.txt**: قائمة المكتبات (حالياً فارغة لأن الكود يستخدم المكتبات القياسية فقط)

### النشر التلقائي (Auto-Deploy)

Railway مفعّل عليه النشر التلقائي افتراضياً، يعني:
- أي تحديث تعمله على GitHub سيتم نشره تلقائياً على Railway
- لا تحتاج إلى رفع الكود يدوياً في كل مرة

### إيقاف أو حذف المشروع

إذا أردت إيقاف المشروع:
1. اذهب إلى **Settings** > **Danger**
2. يمكنك إيقاف المشروع مؤقتاً أو حذفه نهائياً

## روابط مفيدة

- **المستودع على GitHub**: https://github.com/8ta9snm-blip/ai-agent-project
- **موقع Railway**: https://railway.app
- **توثيق Railway**: https://docs.railway.app

## الدعم

إذا واجهت أي مشكلة:
1. تحقق من **Logs** في لوحة تحكم Railway
2. راجع **Deployments** للتأكد من نجاح عملية البناء
3. تأكد من أن جميع الملفات موجودة في المستودع

---

**ملاحظة**: المشروع الحالي هو سكريبت Python بسيط يعمل مرة واحدة ويتوقف. إذا كنت تريد تطبيق يعمل بشكل مستمر (مثل بوت أو API)، ستحتاج إلى تعديل الكود ليعمل كخدمة دائمة.
